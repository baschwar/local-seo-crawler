[package]
name = "local-seo-auditor"
version = "0.3.0"
description = "Local-first SEO audit desktop application"
authors = ["Local SEO Auditor"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
rfd = "0.15"
serde_json = "1"
tauri = { version = "2", features = [] }
